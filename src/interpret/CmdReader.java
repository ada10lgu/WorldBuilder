package interpret;

import java.util.ArrayList;

public abstract class CmdReader {
	
	public ArrayList<Term> command(String cmd){
		ArrayList<Term> terms = new ArrayList<Term>();
		/**
		 * will parse string and create a 
		 * list of term elements that then 
		 * can be executed or used
		 */
		
		return terms;	
	}
}
